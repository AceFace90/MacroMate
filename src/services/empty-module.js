// Empty stub. Metro aliases optional deps that Supabase tries to import for
// server-side tracing (e.g. @opentelemetry/api) to this no-op so the web/native
// bundle doesn't fail resolving a package we don't ship.
module.exports = {};
